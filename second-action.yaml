name: Second Workflow

on:
  workflow_dispatch:

jobs:
  one:
    runs-on: ubuntu-latest

    steps:
      - name: Get OS information
        run: |
          cat /etc/os-release
          date

      - name: Update
        run: sudo apt update -y